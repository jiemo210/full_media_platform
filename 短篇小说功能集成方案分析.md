# AI 短篇小说功能集成方案分析

> 编制日期：2026-08-28　目标：全媒体聚合平台 v2.2（短篇小说创作）
> 篇幅定位：**1 万 ~ 5 万字**中文短篇 + **本地小说库（AI 大纲总结）** + **创作引用库内大纲**

---

## 一、需求理解

1. **创作能力**：AI 生成 1-5 万字中文短篇（题材/设定/大纲/逐章正文），复用富文本编辑与发布中台
2. **本地小说库**：管理创作完成或导入的小说；界面提供 **AI 大纲总结**：
   - 小说人物介绍（姓名/身份/性格/关键作用）
   - 作品主要情节大纲总结（开端→发展→高潮→结局 / 分章摘要）
   - 题材分类（玄幻/都市/悬疑/言情/科幻/历史/其他 + 标签）
3. **参考创作**：新建小说时可引入库内作品的“大纲总结”作为参考（结构、人物设定风格、题材手法），提示词注入但不抄袭

---

## 二、现状盘点（可复用能力）

| 现有能力 | 复用点 |
|----------|--------|
| 多 AI 模型 + 失败自动切换（`ai_service`） | 设定/大纲/章节/总结全部走模型通道 |
| AI 限流 `AI_RATE_LIMIT`（Redis/内存） | 大纲总结、章节生成同样限流防滥用 |
| 富文本编辑器（RichEditor：标题/正文/图片/PDF） | 章节编辑、整本预览 |
| 发布中台（任务/平台规范注入/跳转/复制/PDF） | 小说整本发布（source_type=novel） |
| 写作风格库、素材库、后台管理、JWT 权限 | 风格/封面/管理/权限复用 |

**缺口**：novel 数据模型与路由、创作向导、小说库界面、AI 大纲总结接口、参考注入提示词。

> 参考：原新闻荟聚项目已具备小说雏形（`novel_projects`/`novel_chapters`/`novel_service` 大纲与章节生成），本次设计在其思路上补齐“小说库 + 大纲总结 + 参考创作”。

---

## 三、总体方案与数据流

```
【创作路径】
创作向导（题材/篇幅/风格/参考作品）
    → 设定生成（世界观/角色卡/故事梗概）
    → 大纲生成（分章标题 + 每章要点）
    → 逐章生成（流式，断点续写）→ 富文本编辑 → 整本发布

【小说库路径】
创作完成 / 本地导入 → 入库
    → AI 大纲总结（人物介绍 / 情节大纲 / 题材分类）→ 界面展示
    → 新建小说时“以此为参考创作”→ 参考注入提示词
```

---

## 四、数据模型设计

### 4.0 存储决策（正文=数据库，媒体=磁盘）

| 内容 | 存储 | 原因 |
|------|------|------|
| 章节正文 / 大纲 / 角色卡 / 总结 JSON | 数据库（LONGTEXT） | 结构化文本：逐章编辑、字数统计、续写断点、检索、事务关联 |
| 封面 / 插图 | 磁盘对象存储（`MEDIA_DIR` + `/media`，库中存 URL） | 二进制大文件、可缓存；与富文本插图策略一致 |
| 本地导入原稿（txt/docx） | 磁盘（临时/可选） | 大文件仅解析用，切片后入库 |

1-5 万字纯文本全文约 150KB（单章 3KB 左右），数据库 LONGTEXT 完全胜任，与图片 base64 膨胀问题不同量级。

### 4.0.1 4 万字数据库压力测算与扩展演进

**压力测算**

| 指标 | 数值 |
|------|------|
| 单本 4 万字（UTF-8 中文） | ≈ 120KB；含 md+html 双份 ≈ 240KB |
| 按章（40 章 × 1000 字） | 单章行 3-6KB |
| 1000 本 | ≈ 240MB |
| 10000 本 | ≈ 2.4GB（MySQL 舒适区） |

**使用纪律（比容量更关键）**：①列表只查元数据，正文按需取；②按章存储；③正文不做 LIKE 全表搜索；④备份随量增长用 mysqldump+保留策略。

**技术栈按需演进**

| 技术栈 | 触发阈值 | 建议 |
|--------|----------|------|
| MySQL FTS（ngram） | 需要正文/章节搜索时 | **先上这个**，零新增依赖 |
| Meilisearch/ES | 搜索复杂化、>10 万篇 | 再考虑 |
| MinIO/云 OSS+CDN | 封面/导入/导出文件多、需公网分发 | 生产期替换本地 MEDIA_DIR，正文仍留库 |
| Redis | 限流/进度/结果缓存 | 现有可选依赖，直接启用 |
| RQ/Celery | 批量整本生成/导出、多 worker 重试 | MVP 用线程模式即可 |
| 向量库 | 跨作品参考推荐、长篇一致性语义检索 | 1-5 万字短篇用角色卡+前文摘要足够，暂不上 |

```sql
-- 小说项目（同时也是小说库实体）
novel_projects (
  id, title, genre,                -- 题材（玄幻/都市/悬疑/言情/科幻/历史/其他）
  length_target,                   -- 目标篇幅（1万/3万/5万字）
  synopsis,                        -- 故事梗概
  world_setting,                   -- 世界观设定
  characters JSON,                 -- 角色卡 [{"name","identity","personality","role"}]
  outline JSON,                    -- 章节大纲 [{chapter,title,points[]}]
  summary JSON,                    -- AI 大纲总结 {characters[], plot{start,develop,climax,end}, genre, tags[]}
  reference_ids JSON,              -- 参考作品 id（引用库内大纲总结）
  in_library,                      -- 是否入本地小说库
  status,                          -- draft/writing/completed/imported
  word_count, user_id, created_at, updated_at
)

-- 章节
novel_chapters (
  id, project_id,
  chapter_number, title,
  content, word_count,
  outline,                         -- 本章要点
  status,                          -- draft/generated/edited/finalized
  created_at, updated_at
)
```

**说明**：
- “本地小说库”不新建独立表，`novel_projects.in_library=true` 即入库存档，避免双份数据
- 本地导入：创建 `status=imported` 项目，按 800-1500 字/段切片生成章节（正文落盘，可后续编辑）
- 参考作品用 `reference_ids` 记录，生成提示词时读取其 `summary`

---

## 五、本地小说库 + AI 大纲总结

### 5.1 库界面

- 列表：标题 / 题材分类 / 字数 / 状态 / 是否已生成总结 / 时间 / 搜索（标题+题材）
- 详情页三栏：
  - **人物介绍**：卡片式（姓名/身份/性格/作用）
  - **情节大纲**：开端→发展→高潮→结局 时间线 + 分章摘要
  - **题材分类**：主分类 + 标签
- 操作：生成/重新生成总结、以此为参考创作、导出、发布

### 5.2 AI 大纲总结接口

`POST /api/novels/{id}/summary`（editor+，限流复用 `AI_RATE_LIMIT`）

输入：整本正文（或章节摘要拼接，长文分段汇总）→ 输出 JSON：

```json
{
  "characters": [{"name": "林晚", "identity": "女主/考古学家", "personality": "冷静坚韧", "role": "推动悬疑主线"}],
  "plot": {"start": "...", "develop": "...", "climax": "...", "end": "..."},
  "genre": "悬疑",
  "tags": ["考古", "都市", "女性视角"]
}
```

长文策略：>2 万字时先分块总结→再汇总，控制单次 Token；支持“重新生成”。

---

## 六、AI 创作流程（1-5 万字）

### 6.1 篇幅与章节映射

| 目标篇幅 | 章节数建议 | 单章字数 |
|----------|-----------|---------|
| 1 万字 | 10-15 章 | 800-1000 |
| 3 万字 | 25-40 章 | 800-1200 |
| 5 万字 | 40-60 章 | 1000 左右 |

### 6.2 生成步骤

1. **设定生成**：题材+风格+篇幅 → 世界观/角色卡/梗概（一次调用）
2. **大纲生成**：分章标题+每章要点（一次调用，JSON 输出）
3. **逐章生成**：每章注入“角色卡 + 全书大纲 + 本章要点 + 前文摘要”生成，SSE 流式；支持断点续写
4. **编辑**：章节级富文本编辑（复用 RichEditor），支持“续写/扩写/重写选中段”
5. **发布**：整本或单章转发布任务（平台规范注入沿用）

### 6.3 参考注入（库内大纲总结）

创建/生成时若选择参考作品：

```
参考作品《XXX》大纲总结：
题材：悬疑；人物：林晚(冷静坚韧)...；情节：开端...高潮...
请参考其题材手法、人物设定逻辑与结构节奏，创作**原创**内容，不得抄袭原文表述。
```

- 参考仅用于结构/风格/人物设定逻辑，输出原创正文（合规）
- 可多选参考作品，按相关性注入

---

## 七、API 清单

| 方法 | 路径 | 说明 |
|------|------|------|
| GET/POST | /api/novels | 库列表 / 创建（含导入） |
| GET/PUT/DELETE | /api/novels/{id} | 详情 / 编辑 / 删除 |
| POST | /api/novels/{id}/summary | 生成 AI 大纲总结 |
| POST | /api/novels/{id}/outline/generate | 生成分章大纲（流式/同步） |
| GET/POST | /api/novels/{id}/chapters | 章节列表 / 追加 |
| GET/PUT/DELETE | /api/novels/{id}/chapters/{cid} | 章节详情 / 编辑 / 删除 |
| POST | /api/novels/{id}/chapters/{cid}/generate-content | 生成/续写章节（SSE） |
| POST | /api/novels/{id}/publish | 整本转发布任务 |
| GET | /api/novels/library | 库列表（含总结状态，可筛选“已总结”） |

---

## 八、AI 成本估算与合规

### 8.1 Token 与费用（DeepSeek 系，量级参考）

| 篇幅 | 输出 Token（约） | 含大纲/设定/总结总 Token | 参考费用 |
|------|------------------|--------------------------|----------|
| 1 万字 | 1.5-2 万 | 2-3 万 | ¥0.1-0.4 |
| 3 万字 | 4.5-6 万 | 6-8 万 | ¥0.4-1.2 |
| 5 万字 | 7.5-10 万 | 10-13 万 | ¥0.6-2 |

- 已有限流（每用户/小时）+ 建议增加“章节生成确认”防误触
- 大纲总结对长文分块汇总，控制成本

### 8.2 合规

- 参考作品仅注入“大纲总结（结构/人物设定逻辑）”，输出为原创正文
- 界面标注“AI 生成”，发布合规提示沿用
- 导入外部小说仅作个人学习参考，不对外发布侵权内容

---

## 九、前端页面设计

| 页面 | 功能 |
|------|------|
| 小说创作向导 | 题材/篇幅/风格/参考作品（从库选择）→ 设定 → 大纲 → 开始创作 |
| 小说编辑器 | 章节树 + 富文本编辑器 + 大纲视图 + 角色卡 + 生成/续写按钮 |
| 小说库 | 列表 + 详情（人物/情节/题材三栏）+ 生成总结 + 以此为参考创作 |
| 后台小说管理 | 列表/删除/生成总结/导出（并入现有后台） |

复用：RichEditor、Toast、多模型选择、风格库、发布中台、后台布局。

---

## 十、实施计划（1 人全栈，约 2 周）

| 阶段 | 内容 | 周期 |
|------|------|------|
| 1 后端基础 | novel 表 + 路由 + 导入切片 + 大纲总结接口 | 3-4 天 |
| 2 AI 创作 | 设定/大纲/章节生成 + 参考注入 + 断点续写 | 3-4 天 |
| 3 前端 | 创作向导 / 编辑器 / 小说库三页面 | 3-4 天 |
| 4 集成验收 | 发布集成（source_type=novel）+ 回归 + 文档 | 2 天 |

---

## 十一、风险与建议

| 风险 | 影响 | 应对 |
|------|------|------|
| 长篇生成 Token 成本 | 成本上升 | 分批生成、章节确认、限流复用 |
| 人物一致性漂移 | 质量下降 | 角色卡注入每章提示词 + 前文摘要 |
| 大纲总结长文截断 | 总结不全 | 分块总结→汇总；章节摘要作为输入 |
| 抄袭合规 | 法律风险 | 参考仅结构/风格，输出原创声明 |
| 导入文本编码/分段 | 导入异常 | 按段落与字数切片，兼容常见编码 |
| 章节生成中断 | 体验差 | SSE 断点续写 + 已生成内容落库 |

---

## 十二、结论

1. **方案可行且复用度高**：多模型/限流/富文本/发布/后台全部可复用，新增集中在 novel 数据模型、创作管线与小说库界面
2. **建议最小闭环先行**：小说库（导入 + AI 大纲总结 + 详情展示）→ 参考创作（向导选参考 → 设定/大纲/首章生成）→ 逐步扩展到整本
3. **与路线图一致**：对应 v2.2 短篇小说方向，可在 v2.1 视频方向后或并行启动

## 十三、实施状态（2026-08-28 已落地）

| 模块 | 状态 |
|------|------|
| 数据模型（novel_projects / novel_chapters） | ✅ |
| 后端路由（CRUD/导入/设定/大纲/章节生成 SSE/总结/发布） | ✅ |
| AI 大纲总结（人物/情节/题材） | ✅ 实测生成 |
| 参考注入（build_reference_text） | ✅ 实测生效 |
| 前端：小说库 / 创作向导 / 编辑器 / 后台小说管理 | ✅ 页面验收通过 |
| 发布集成（整本转发布任务） | ✅ |
| 生产构建 | ✅ |

---

**文档结束**
