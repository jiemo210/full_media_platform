# 全媒体聚合平台 · 上线评估与优化建议

> 编制日期：2026-08-28　评估对象：full_media_platform v2.0

---

## 一、图片存储现状与合理性分析

### 1.1 当前实现

| 图片类型 | 存储方式 | 说明 |
|----------|---------|------|
| 富文本正文图片 | **base64 内嵌数据库**（`articles.content_md` / `content_html`，LONGTEXT） | 编辑器插入/粘贴图片 → FileReader 转 data URL → 存入 Markdown 与 HTML |
| 新闻缩略图 | 外部 URL 直链（`news.image_url`，String 500） | 引用新闻源原图，未落盘 |
| 发布任务正文 | base64 内嵌（`publish_tasks.content` / `package_text`，LONGTEXT） | 由文章复制而来 |

### 1.2 数据库存图片（base64）的合理性评估

**结论：当前方式适合开发/演示，不适合真实上线（尤其多用户、多图片场景）。**

**优点（为什么现在能跑）**

- 零基础设施：不需要文件服务/对象存储/CDN
- 数据自包含：导出、迁移、复制简单
- 开发期数据量小（实测文章正文最大约 6KB，尚无大图），未触发瓶颈

**缺点（上线后会放大）**

| 问题 | 影响 |
|------|------|
| 体积膨胀 ~33% | base64 编码使图片体积增加约 1/3，且 `content_md` + `content_html` 各存一份 |
| 数据库膨胀 | 一张 2MB 照片约 2.7MB 入库，文章/任务量上来后库体量失控，备份变慢 |
| 查询性能劣化 | 列表/导出等全表操作扫描 LONGTEXT；InnoDB 缓冲池被大字段占用，命中率下降 |
| 单包上限风险 | 单条 insert 受 `max_allowed_packet`（当前 64MB）限制，多图文章可能触顶报错 |
| 无法缓存/CDN | 图片只能走 API 传输，浏览器/CDN 无法高效缓存，首屏与带宽成本高 |
| 无法做缩略图/裁剪 | 需全文传原图，移动端体验差 |
| 与“素材复用”冲突 | 漫画/视频封面等后续需求要求图片作为独立资产可复用，数据库内嵌无法复用 |

### 1.3 推荐改造方向

1. **引入对象存储抽象**（本地磁盘 / MinIO / 阿里云 OSS 三选一，先本地 + 预留适配层）
2. 新增图片上传接口（`POST /api/media/upload`），返回 URL
3. 富文本编辑器插入图片改为**上传后引用 URL**（保留粘贴 data URL 的降级，但入库前转存）
4. 数据库只存 URL；正文中的图片 URL 指向 `/media/...` 或 OSS 域名
5. 存量 base64 数据提供一次性迁移脚本（扫描 `data:image` → 转存文件 → 替换为 URL）
6. 新闻缩略图建议做本地代理缓存（避免外链失效与防盗链 403）

---

## 二、真实上线的瓶颈盘点

### 2.1 数据与存储

- **图片全部走数据库**：见第一章，最大瓶颈
- **无素材库/媒体资产表**：后续视频、漫画、封面复用无落点
- **正文大字段与业务同表**：`articles`/`publish_tasks` 直接存大文本，列表查询开销高；建议正文与元数据分表或使用 LONGTEXT 前先评估量级
- **搜索用 LIKE**：`%关键词%` 无法走索引，600 条时尚可，上万条会变慢；建议升级 MySQL FTS 或引入 Meilisearch

### 2.2 爬虫

- **同步抓取阻塞请求**：`POST /api/news/crawl` 同步执行 8 源抓取（含随机 sleep），慢源时接口可能耗时 30-60s，前端等待且可能超时；应改为后台任务 + 进度查询
- **去重查询逐条执行**：每条标题先查库再插入，量级上来后 N+1 查询明显；建议标题哈希唯一索引 + 批量 upsert
- **源稳定性**：源页面结构变化会导致 0 条，当前靠手动健康探测；建议抓取失败自动降级/告警
- **无反爬对策**：高频抓取可能被封 IP；生产需代理池/限频/UA 轮换（低频场景可先不管）

### 2.3 AI 服务

- **密钥明文落盘**：`AI_MODELS` 的 `api_key` 会随后台保存写入 config.json 明文；建议只存密文或引用环境变量
- **无失败降级**：单一模型失败无自动切换；建议按 AI_MODELS 顺序重试
- **无调用频控/成本控制**：任何 editor 可无限调用 AI（生成/改写/建议），存在成本与滥用风险；建议按用户/小时限流 + 用量统计
- **无结果缓存**：相同新闻重复改写建议未缓存（旧项目有 Redis 缓存，新项目未接）；建议接入 Redis 缓存改写建议与常用 Prompt 结果

### 2.4 发布与任务

- **发布依赖人工跳转**：当前是“跳转发布页 + 复制”，符合双轨策略，但无官方 API 直发与回执数据；上线初期可接受，需明确边界
- **无定时/重试**：发布任务无排期与失败重试（新项目简化版）；若要做矩阵发布需补齐
- **无发布频率限制**：可创建任意数量任务，缺保护

### 2.5 安全与合规

- **默认口令与空密码**：管理员 admin/admin123；MySQL root 空密码；上线必须修改并环境变量化
- **无审计日志**：用户/配置/发布操作无留痕（原项目有 audit_logs，新项目未带）
- **密钥管理**：JWT/API Key 存 .env 明文；生产建议密钥管理服务或加密配置
- **HTTPS 缺失**：部署需 TLS；CORS 白名单需收敛
- **AI 内容标识**：未强制标注 AI 生成内容（合规风险）
- **XSS 面**：富文本/预览使用 v-html；当前渲染会转义 Markdown 源码，风险可控，但仍建议对 `content_html` 渲染做白名单消毒

### 2.6 稳定性与运维

- **进程易中断**：期间已多次出现后端进程被杀/端口被占、前端 dev 断连；生产需进程守护（NSSM/计划任务/服务化）
- **无自动备份**：MySQL 无定时备份脚本；需 mysqldump + 保留策略
- **单实例**：无横向扩展、无高可用；初期可接受，但数据库连接池参数（pool_size）需按并发调优
- **日志**：已有访问/业务日志，但无日志采集与告警（如需可接 filebeat/EFK）
- **前端部署**：dev server 仅限开发；上线需 `npm run build` + 静态托管 + 反向代理 /api 与 /media

### 2.7 成本

- AI Token 成本：流式生成长文耗 Token，批量/多人使用成本线性增长；需限流 + 用量看板
- 图片带宽/存储成本：数据库内嵌使存储与传输成本虚高；转对象存储 + CDN 可显著降本

---

## 三、关注点清单（按优先级）

### P0（上线前必须）

- [x] 修改默认口令（admin / MySQL root），密钥环境变量化（待部署时执行）
- [x] 图片存储改造：本地素材库 + 上传接口 + URL 入库（富文本插图自动上传；可配置 MEDIA_DIR）
- [x] 爬虫改后台任务（异步执行 + 状态查询），避免同步阻塞
- [x] AI 调用限流（按用户/小时）+ 模型失败自动切换
- [x] 发布频率限制 + 任务数上限
- [ ] MySQL 定时备份 + 恢复演练
- [ ] 进程守护与启动脚本（后端/前端服务化）
- [ ] HTTPS + CORS 收敛 + 生产构建部署

### P1（上线后 1-2 个月）

- [ ] 审计日志（登录/配置/发布/删除）
- [ ] 搜索升级（MySQL FTS / Meilisearch）
- [ ] 爬虫去重哈希索引 + 批量 upsert + 失败告警
- [ ] 改写建议 Redis 缓存 + AI 用量统计看板
- [ ] 素材库/媒体资产表（为视频/漫画铺路）
- [ ] 正文与元数据分表或列存储评估
- [ ] 定时发布/重试/数据回填（官方 API 接入）

### P2（方向性演进）

- [ ] 视频脚本/口播/分镜（v2.1）
- [ ] 短篇小说创作（v2.2）
- [ ] AI 漫画创作（v2.3，图片资产依赖 P0 存储改造）
- [ ] 平台矩阵与数据回填（v2.4）
- [ ] 团队协作、细粒度权限、用量计费

---

## 四、分阶段建议

| 阶段 | 重点 | 说明 |
|------|------|------|
| 第 1 阶段（上线） | 图片存储改造 + 安全基线 + 异步爬虫 + 限流 | 解决“能跑”到“能上线”的硬伤 |
| 第 2 阶段（稳定） | 审计/搜索/缓存/备份自动化 | 解决“能用”到“好用” |
| 第 3 阶段（增长） | 素材库 → 视频/小说/漫画 + 平台矩阵 | 按产品路线图推进 |

---

**文档结束**
