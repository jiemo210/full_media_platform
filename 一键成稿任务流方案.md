# 一键成稿任务流 · 需求分析与改进计划

> 文档版本：v0.2　编制日期：2026-09-09　状态：评审稿（M1 已实现，M2/M3 待排期）
> 关联文档：全媒体聚合平台产品文档_v2.0.md、上线评估与优化建议.md、短篇小说功能集成方案分析.md

---

## 一、业务目标与用户场景

**一句话定位**：把“选题 → 找资料 → 成文 → 风控 → 修订 → 入库/发布”从手动串联，升级为**一次点击的编排任务**——过程可见、可干预、可续跑、可批量。

### 典型场景

1. **热点一键成稿**：运营在热点新闻点“一键成稿”，约 60–90 秒后得到一篇已通过风控、可发布或已入库的文章。
2. **自由主题成稿**：创作者输入主题，系统自动检索资料、成文；可在“素材/正文/风控结果”处停留编辑后再继续。
3. **多平台变体**：同一主题基于同一套素材与事实核查结果，生成多平台适配版本（头图/字数/风格不同）。
4. **批量作业**：运营把一批选题加入队列，后台依次自动成稿（配合模板与用量控制）。

---

## 二、现状可复用能力盘点

| 能力 | 现有实现 | 复用方式 |
|------|---------|---------|
| 素材获取 | 资料搜索（360/Bing + 本地热点 + AI 精选，含时间窗）、新闻正文抓取 | 作为任务第一阶段 |
| 成文 | AI 创作 / AI 改写（SSE 流式、多模型失败切换、平台规范注入） | 封装为任务阶段 |
| 风控 | risk-check（法规 + 平台风控，结构化 JSON 结果） | 固定阶段 |
| 修订 | risk-revise（按风险点/建议流式改写） | 高风险自动修订（轮数可配） |
| 保存 / 发布 | 文章库接口、publish_service 批量任务 | 终态落库、创建发布任务 |
| 限流 | 每用户/小时 AI 与发布限流 | 任务执行器沿用 |
| 异步基础 | 异步爬虫 + 状态查询、全局爬虫锁 | 执行器模式参考 |
| 前端 | SSE 公共封装、RichEditor、Toast、风控面板 | 任务进度与产物编辑直接复用 |
| 迁移 | migrations.py 轻量迁移机制 | 新增任务表迁移 |

---

## 三、需求拆解

### 3.1 功能需求

**FR-1 任务创建**
- 入口：热点新闻详情“一键成稿”、独立“成稿任务”页自由主题输入。
- 参数：题材来源（news_id / 自由主题）、写作风格、字数、AI 模型、目标平台、风控开关、修订轮数、完成后动作（仅入库 / 入库并建发布任务）。
- 模式：`自动`（全自动直到终态）或 `分步`（每阶段产物确认后继续）。

**FR-2 阶段编排（v1 文章任务）**

```
输入(新闻/主题) → 素材收集 → [事实核查*] → [大纲*] → 成文
   → 风控 → [自动修订(≤N 轮)] → 定稿入库 → [创建发布任务]
```

- v1 默认链路最短：**素材 → 成文 → 风控 → 修订 → 入库**。
- `*` 标注项为 M2 增强：事实核查、文章大纲。
- 输入为热点新闻时，素材阶段直接抓原文，跳过联网搜索。
- 每个阶段为独立可开关单元，失败不连带污染其他阶段产物。

**FR-3 任务状态与生命周期**
- 任务级：`queued → running → waiting_user ↺ running → completed`，异常进入 `failed`，支持 `cancelled`。
- 阶段级：`pending → running → done / failed / skipped`。
- 失败默认“中止 + 本阶段重试”；取消仅在阶段边界生效。
- 服务重启后从最后成功阶段续跑（产物已落库，具备幂等性）。

**FR-4 产物管理**
- 每阶段产物独立入库（正文 LONGTEXT），含来源、模型、调用次数、耗时等元数据。
- 分步模式下，用户可在 RichEditor 中修改产物后“确认继续”。

**FR-5 进度与通知**
- SSE 实时推送阶段事件（阶段开始/结束、token 流、错误）；页面刷新后从 DB 恢复。
- 完成通知：Toast/站内提醒，一键跳转文章库或发布页。

**FR-6 用量与成本可见**
- 任务详情展示 AI 调用次数、估算字数/费用（若接用量统计）。
- 执行前按预估调用数做限流校验；修订轮数默认上限 1。

### 3.2 非功能需求

- **稳定性**：单任务失败不影响其他任务；重启不丢任务、不重复执行。
- **限流安全**：与现有 AI/发布限流共用额度，任务不绕过限流。
- **性能**：MVP 单实例单 Worker 串行执行即可；交互不阻塞。
- **兼容**：保留创作/改写原入口，不回归既有功能。
- **合规**：素材保留原文链接与抓取时间；成文提示词限制改写事实；AI 内容标识纳入元数据。

---

## 四、数据模型草案

```sql
pipeline_runs
  id            BIGINT PK AUTO_INCREMENT
  user_id       INT          -- 创建人
  source_type   VARCHAR(20)  -- rewrite / create
  news_id       INT NULL
  topic         VARCHAR(300) -- 自由主题（非 news 输入时）
  mode          VARCHAR(10)  -- auto / step
  config        JSON         -- 风格/字数/平台/模型/风控开关/修订轮数/完成动作
  status        VARCHAR(20)  -- queued/running/waiting_user/completed/failed/cancelled
  current_stage VARCHAR(30)
  worker_id     VARCHAR(64)  -- 执行租约，防多实例重复执行
  ai_calls      INT DEFAULT 0
  error         VARCHAR(500)
  created_at / updated_at / finished_at
  KEY idx_user_status (user_id, status)
  KEY idx_status (status)

pipeline_stage_artifacts
  id            BIGINT PK AUTO_INCREMENT
  run_id        BIGINT
  stage         VARCHAR(30)  -- material/draft/risk/final 等
  order_no      INT
  title         VARCHAR(300)
  content_md    LONGTEXT
  content_html  LONGTEXT
  meta          JSON         -- 素材来源/风控结果/模型/字数/耗时
  status        VARCHAR(20)  -- pending/running/done/failed/skipped
  updated_at
  KEY idx_run (run_id)
```

> 迁移通过 `backend/migrations.py` 追加 `m005_pipeline_tasks` 实现，沿用现有幂等机制。

---

## 五、执行器技术方案

### 5.1 MVP：进程内 Worker + DB 状态（推荐先落地）

- 全局顺序 Worker：同一时刻只执行一个任务，天然控制 AI 并发与限流压力。
- 领取机制：扫描 `status='queued'` 行 → 置 `running + worker_id`（原子更新）→ 执行。
- 阶段执行器抽象：`stage.execute(run, ctx)`，每阶段独立 try/except 与重试入口。
- 每阶段 AI 调用前按预估次数 `check_limit + record_hit`（与文章接口一致）。
- 启动钩子：应用启动时扫描 `queued/running` 的任务续跑；`running` 且 `worker_id` 过期视为中断恢复。
- SSE 推送：任务级事件订阅（阶段开始/结束/token/错误），页面刷新后回放 DB 产物。

### 5.2 演进：分布式队列（预留）

多实例 / 大批量时切换 Redis 队列或 ARQ/RQ，仅替换“领取/执行”层，任务表结构不变。

---

## 六、前端 UX 设计

### 6.1 页面结构

- **入口**：导航“成稿任务”；热点新闻操作区“一键成稿”。
- **任务列表**：卡片展示进度条、当前阶段、耗时、AI 调用数、状态；失败显示原因与“重试本阶段”。
- **任务详情**：
  - 阶段时间线（素材 → 成文 → 风控 → 修订 → 入库）；
  - 每阶段产物可在 RichEditor 展开编辑；
  - 风控结果复用现有风险面板（级别/问题/建议/“AI 按建议修改”）；
  - “从此阶段重跑”“确认并继续”“取消”。
- **创建弹层**：风格/字数/平台/模型/模式/完成动作；常用参数保存为“配方模板”（M2）。

### 6.2 交互原则

- 自动模式：全程无阻塞，后台推进，完成后通知。
- 分步模式：在 `waiting_user` 状态暂停并提示人工确认；人工编辑产物不影响已完成阶段。
- 实时性：SSE 推送；弱网/刷新降级为轮询或 DB 快照展示。

---

## 七、里程碑与工作量估算

| 里程碑 | 交付内容 | 周期 | 验收标准 |
|--------|---------|------|---------|
| M1 MVP | ✅ 已实现：任务表 + 状态机 + Worker；默认链路（素材→成文→风控→修订→入库）；任务列表/详情/SSE 进度；失败重试、取消、重启续跑 | 1–2 周 | 一键 60–90s 产出已过风控文章并入库；断线/重启可续；不绕过限流 |
| M2 协同增强 | 分步确认模式（产物编辑后继续）、事实核查阶段、一键发布联动、用量统计、配方模板 | 2–3 周 | 高风险内容先修订、人工确认后再发布；模板化批量跑 |
| M3 生产化 | 并发上限与优先级、多平台变体并行、异常告警、接入用量看板与数据回填 | 3–4 周 | 稳定支撑日常矩阵产出；任务异常可观测、可告警 |

---

## 八、关键风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| 链路越长失败面越大 | 一键任务中途失败率高 | 阶段独立可重试；产物即时落库；默认最短链路；单阶段续跑 |
| AI 成本不可控 | 批量使用时费用线性上升 | 阶段前限流校验；修订轮数上限；任务内用量展示 |
| 时长体验差 | 用户等待焦虑 | SSE 实时推进 + 阶段耗时；自动模式后台运行、完成通知 |
| 内容事实与版权 | 改写偏离事实、来源缺失 | 素材保留原文链接/抓取时间；成文约束不改事实；M2 增加事实核查清单 |
| 与既有功能冲突 | 回归风险 | 任务流只新增编排层，创作/改写原入口保留；复用同一套 AI/风控/发布模块 |
| 多实例重复执行 | 生产部署后任务双跑 | DB 租约（worker_id + 状态原子更新），预留分布式队列切换 |

---

## 九、落地顺序建议

1. **先做 M1 最小闭环**：热点新闻 → 自动成稿（含风控与自动修订）→ 文章库。
2. 验证价值后增加**分步确认模式与人工编辑产物**，补齐事实核查与一键发布。
3. 最后扩展**配方模板、多平台变体、批量队列与用量看板**。

---

**文档结束**
